---
lens: NAME
topics: []                  # primary topics, from the CLAUDE.md §7 taxonomy
types: all                  # all | a subset: [take, story, lesson, fact]
visibility-ceiling: public  # public | agents-only | private
identity: [core, beliefs]   # which identity files this lens loads
---
# Lens: NAME

One line: which consumer or task this lens is for.

- primary topics: [...]
- secondary: any topic when the task clearly needs it (lenses focus, they
  don't censor)
- note types: all
- visibility ceiling: public          # why: e.g. serves a public audience
- identity: core + beliefs
